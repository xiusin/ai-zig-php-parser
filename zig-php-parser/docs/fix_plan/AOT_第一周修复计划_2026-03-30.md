# AOT模糊测试修复 - 后续开发建议

**生成时间**: 2026-03-30  
**当前通过率**: 47.4% (154/325)  
**目标通过率**: 85%+ (276/325)  
**需修复**: 122个脚本

---

## 🎯 优先级矩阵 (按ROI排序)

| 优先级 | 类别 | 数量 | 预计工时 | 通过率提升 | ROI |
|--------|------|------|---------|-----------|-----|
| **P0** | P1编译错误 | 7 | 20h | +4.2% | ⭐⭐⭐⭐⭐ |
| **P1** | P2高频函数 | 10 | 16h | +6.2% | ⭐⭐⭐⭐⭐ |
| **P2** | P5展开运算符 | 3 | 8h | +1.8% | ⭐⭐⭐⭐ |
| **P3** | P4类数组常量 | 12 | 24h | +7.4% | ⭐⭐⭐⭐ |
| **P4** | P7引用传递 | 2 | 8h | +1.2% | ⭐⭐⭐ |
| **P5** | P7闭包绑定 | 4 | 12h | +2.5% | ⭐⭐⭐ |
| **P6** | P5动态变量 | 3 | 10h | +1.8% | ⭐⭐ |
| **P7** | P3重复声明 | 15 | 16h | +9.3% | ⭐⭐ |

---

## 📋 第一周任务清单 (40小时)

### Day 1-2: P0编译错误修复 (20h)

#### 任务1.1: 引用参数类型统一 (4h)
```bash
# 问题: test_058_object_graph.php
# 错误: expected type 'Value', found '*Value'

# 修复步骤:
1. 定位: src/aot/ir_generator.zig:879-885
2. 分析: 引用参数生成 alloca 时类型不一致
3. 修复: 统一引用参数的IR类型表示
4. 测试: zig build && ./test_single.sh test_058_object_graph.php
```

**代码位置**:
```zig
// src/aot/ir_generator.zig:879
if (param_data.is_reference) {
    // 当前: 生成 *Value
    // 应该: 生成 Value，在使用时解引用
}
```

#### 任务1.2: Reflection方法补全 (8h)
```bash
# 缺失方法:
- ReflectionMethod::getParameters()  # test_089
- ReflectionAttribute::newInstance() # test_065
- ReflectionProperty相关方法        # test_040

# 修复位置: src/aot/runtime_lib.zig
# 参考: PHP官方Reflection API文档
```

#### 任务1.3: 动态方法调用修复 (6h)
```bash
# 问题: test_025_callables.php, test_039_variable_funcs.php
# 错误: error: UnknownFunction in aot_dispatch

# 修复步骤:
1. 分析: First-class callable语法生成的IR
2. 修复: src/aot/ir_generator.zig 动态调用分发逻辑
3. 添加: 运行时方法查找fallback机制
```

#### 任务1.4: 静态方法调用修复 (2h)
```bash
# 问题: test_244_stack.php
# 错误: MethodNotFound in php_call_static_with_ctx

# 快速修复: 检查静态方法查找顺序
```

---

### Day 3-4: P1高频函数实现 (16h)

#### 任务2.1: 数学/日期函数 (4h)
```zig
// src/aot/runtime_lib.zig

pub fn php_log2(value: Value, allocator: Allocator) !Value {
    const num = try value.toFloat();
    return Value.fromFloat(@log2(num));
}

pub fn php_getdate(timestamp: ?Value, allocator: Allocator) !Value {
    // 返回关联数组: seconds, minutes, hours, mday, wday, mon, year, yday, weekday, month
    const ts = if (timestamp) |t| try t.toInt() else std.time.timestamp();
    // 实现日期解析逻辑
}
```

#### 任务2.2: SPL迭代器函数 (6h)
```zig
pub fn php_iterator_to_array(iterator: Value, use_keys: Value, allocator: Allocator) !Value {
    // 将Iterator转换为数组
    // 影响: test_059_filter_iterator.php 等2个脚本
}
```

#### 任务2.3: 数组查找函数 (2h)
```zig
pub fn php_array_find(array: Value, callback: Value, allocator: Allocator) !Value {
    // PHP 8.4新函数
    // 返回第一个满足条件的元素
}
```

#### 任务2.4: 哈希函数 (4h)
```zig
pub fn php_password_hash(password: Value, algo: Value, options: Value, allocator: Allocator) !Value {
    // 使用bcrypt算法
}

pub fn php_hash_algos(allocator: Allocator) !Value {
    // 返回支持的哈希算法列表
}
```

---

### Day 5: P2展开运算符支持 (8h)

#### 任务3.1: 解析器支持 (4h)
```zig
// src/compiler/parser.zig

// 当前问题: unexpected variable "$nums", expecting ")"
// 需要支持: function(...$args) 和 func(...$array)

fn parseFunctionCall() !Node {
    // 添加展开运算符识别
    if (self.match(.ellipsis)) { // ...
        const expr = try self.parseExpression();
        // 生成 spread 节点
    }
}
```

#### 任务3.2: IR生成支持 (4h)
```zig
// src/aot/ir_generator.zig

fn generateFunctionCall(call_node: Node) !Register {
    // 检测spread参数
    for (args) |arg| {
        if (arg.tag == .spread) {
            // 展开数组为多个参数
            // 生成运行时展开代码
        }
    }
}
```

**影响脚本**: test_012_variadic_splat.php, test_050_spread.php, test_130_spread_method.php

---

## 🔧 快速修复脚本

创建自动化测试脚本：

```bash
#!/bin/bash
# quick_fix_test.sh

SCRIPTS=(
    "test_058_object_graph.php"
    "test_089_reflection2.php"
    "test_065_attribute_reflection.php"
    "test_025_callables.php"
    "test_039_variable_funcs.php"
    "test_244_stack.php"
    "test_016_math.php"
    "test_013_datetime.php"
    "test_059_filter_iterator.php"
    "test_117_array_find.php"
)

echo "=== 快速修复测试 ==="
for script in "${SCRIPTS[@]}"; do
    echo "Testing: $script"
    timeout 10 ./zig-out/bin/php-interpreter --compile "fuzzy_scripts/$script" 2>&1 | head -5
    if [ $? -eq 0 ]; then
        echo "✅ PASS"
    else
        echo "❌ FAIL"
    fi
    echo "---"
done
```

---

## 📊 第一周预期成果

| 指标 | 当前 | 目标 | 提升 |
|------|------|------|------|
| 通过率 | 47.4% | 62%+ | +14.6% |
| 编译错误 | 12个 | 5个 | -7个 |
| 函数缺失 | 35个 | 25个 | -10个 |
| 语法错误 | 22个 | 19个 | -3个 |

---

## 🚨 风险提示

1. **引用参数修复**: 可能影响现有通过的脚本，需全量回归测试
2. **Reflection实现**: 需要深入理解PHP Reflection API，工时可能超预期
3. **展开运算符**: 涉及解析器和IR生成器双重修改，需谨慎测试

---

## 📝 下一步行动

**立即执行**:
```bash
# 1. 创建修复分支
git checkout -b fix/aot-fuzzy-test-p0-p1

# 2. 开始第一个任务
cd src/aot
# 修复 test_058_object_graph.php 引用参数问题

# 3. 编译测试
zig build
timeout 10 ./zig-out/bin/php-interpreter --compile fuzzy_scripts/test_058_object_graph.php

# 4. 验证输出
./test_058_object_graph
```

---

## 📚 参考资料

- [PHP Reflection API](https://www.php.net/manual/en/book.reflection.php)
- [PHP 8.4 array_find RFC](https://wiki.php.net/rfc/array_find)
- [Zig 0.15.2 API变更](https://ziglang.org/download/0.15.2/release-notes.html)
- [AOT编译器架构文档](../architecture/AOT_COMPILER_ARCHITECTURE.md)

---

**预计完成时间**: 2026-04-06 (第一周)  
**下次评审**: 每完成一个P0任务后进行代码审查
