---- PAGE 1 ------
## 关于项目`AGENTS.md`
现在市面很多IDE均支持且默认明确加载宪法文件，当时在工作中使用Windsurf时发现它不会默认始终使用，如果项目内只想使用宪法文件且想以此作为全局rule使用，头部需要添加yaml配置：
```yaml
---
trigger: always_on
---
```

### 什么是 AI 的渐进式披露（Progressive Disclosure）
渐进式披露是一种源自用户体验设计、在人工智能领域被重新定义的重要设计原则和架构策略。其核心理念是：不一次性向用户或模型展示所有信息和功能，而是根据需求分阶段、按需逐步揭示复杂性。

🛠️ 主要应用场景
1️⃣ 用户界面设计（UX 层）
新手引导：先展示核心功能，高级设置折叠隐藏
复杂工具：如 Loom 先显示基础剪辑，点击"更多"才展示 AI 转录等高级功能
防决策瘫痪：避免用户面对过多选项无从下手

2️⃣ AI Agent 架构（技术层）
上下文管理：用有限 Token 管理无限信息，避免"上下文爆炸"
工具路由：分层展示工具菜单（如先选"数据库类"，再显示 sql_query 等具体工具）
流程控制：SOP 执行中"流程锁"机制——上一步未通过，下一步工具不披露

3️⃣ 企业级落地（业务层）
Claude Skills / Agent Skills：将专业知识打包成模块，仅在相关意图触发时动态加载
车载 AI / RAG 系统 / 企业协作：按场景分层设计，提升响应效率与准确性

💡 一句话总结：渐进式披露是让 AI 系统"像人类专家一样思考"的架构哲学——不堆砌信息，而是在恰当的时机，提供恰到好处的能力。

#### 实践
1. 宪法是渐进式披露的“守门人”和“调度员”。

```markdown
# 📜 项目宪法 (Project Constitution)

## 1. 全局原则 (Always On)
- 语言：TypeScript
- 风格：Functional Programming
- 禁止：任何直接操作生产数据库的 SQL 语句

## 2. 渐进式披露规则 (Progressive Disclosure Rules)

### [模式: 架构设计]
- **触发词**: "design", "architecture", "plan"
- **允许访问**:
  - `/docs/architecture.md`
  - `src/interfaces/*.ts`
- **禁止访问**:
  - `src/internal/logic/*` (细节隐藏，防止过早优化)
- **输出限制**: 仅输出流程图描述和接口定义，禁止生成实现代码。

### [模式: 代码实现]
- **触发词**: "implement", "fix", "write code"
- **允许访问**:
  - 当前文件全文
  - 直接依赖的文件 (通过 AST 解析动态加载)
- **工具解锁**:
  - `file_write` (需用户确认)
  - `terminal_run_test` (仅限测试环境)

### [模式: 危险操作]
- **触发词**: "delete", "drop", "migrate"
- **强制流程**:
  1. 解释风险。
  2. 生成备份脚本。
  3. **必须**等待用户输入 "CONFIRM_RISK" 字符串才能披露执行命令。
```



## 发现新的玩意
- Solo模式
- /spec 或 /plan 指令， 和windsurf的 plan模式类似；
- zen视图，只保留AI对话和输出，有用过Qoder的应该不陌生；
- 变更审查
- 问题发现与重构建议

#### Trae的使用总结
balabala....


#### 怎么在Windsurf上使用
见过猪跑了那就吃点猪肉， 分析他们的总结文档内容，是什么样的，我们在宪法里添加对应的规则
 **可视化**：每个大规模任务完成后都要为我们生成一个变更摘要文档 `docs/{{DATE}}_{{会话模型}}_{{类型}}_{{HASH}}.md`，要求包含如下结构 ⚠️ AI禁止阅读：
   ```
   1. 高层摘要(TL;DR)
   2. 影响范围 (表格/图表/结构化/影响描述)
   3. 核心变更 (表格/图表/结构化/变更描述)
   4. 可视化概览 (代码与逻辑映射)【mermaid视图:Code & Logic Map】
      - 业务模块/业务目标/分层架构图
      - 业务流程 
      - 执行流程
      - 变更点业务相关业务代码逻辑/从上层到底层时序图
   5. 详细变更分析 (表格/图表/结构化)
      - 端/模块层/业务层/数据层等
      - 涉及文件列表
      - 变更点描述
   6. 影响与风险评估
      - 是否破坏式变更
      - 变更影响范围及明细
      - 需要特别注意的点
      - 复测路径（体验流程）
   7. 是否有遗留问题/潜在问题
   8. 后续开发/优化建议
   ```

- 为什么禁止AI读呢？  AI会习惯性的扫描并读取这类文件做参考；
- 需要的插件：`yzhang.markdown-all-in-one` 和 `bierner.markdown-mermaid`
#### 结果呈现：
balabala....

#### 问题
1. AI不知道如何定义重大变更，生成这样的文档会浪费token(最浪费的时候可能就是上下文正好在总结文档超出)；
   可以配置成单独的文件或规则或写一个SKILL来自动触发使用一些常规模型总结输出（这个我没实践过）理论可行；
2. 一般触发方式是Opus写完以后或提交，我们选中提交记录让`SWE-1.5`模型做总结；或者直接点开trae点开让它总结变更/和发现问题（这个也是trae启发式功能）；

### 延伸
可以把变更记录的文档结构样板调整为项目功能架构等模块链路架构的分析文档‘可视化’，更专业更可靠；



