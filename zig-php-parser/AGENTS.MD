你正在维护一个 Zig 0.15.2 的 PHP AOT 编译器项目。

关键约束：（必须遵守）
1. Zig 0.15.2 API — ArrayList/HashMap 全部需要 allocator 参数
2. @intCast/@truncate/@ptrCast 都是单参数语法
3. 跨目录导入通过 build.zig 模块，禁止 ../ 路径
4. 修改代码前必须先读取目标文件的相关上下文
5. 不要凭记忆写代码，每个函数调用都要有依据
6. 编译验证：timeout 120 zig build
7. 测试执行：timeout 10 <command>
8. native_linker.zig 有 664KB，ir_generator.zig 有 234KB
   — 修改前必须 grep 搜索已有模式
   — 禁止凭推测写代码
   - 禁止未知意图的情况下编写代码
```

接下来请完全遵守宪法目录 `constitution` 中的所有规则

## 必要限制（必须遵守）
> Zig开发箴言：**"如果编译器不能证明它是安全的，那它就是不安全的"**  
> 所有代码必须通过`zig build test`验证，性能关键路径必须包含基准测试数据。
> 不可简化实现，打桩实现，必须完整实现所有功能。
> 请在交互过程称呼我为 “老铁”;
> 必须保证代码通过完整通过编译，通过功能点测试！！！！
> DRY原则：不要重复 
> KISS原则：保持简单
> 以性能，算法，巧妙实现为第一要素，从底层到CPU指令的高级用法来优化；
> 模块开发进度要及时更新(ai可参考进度的文档)，如：“{模块/功能}-开发进度总结报告.md”;
> 必须考虑性能，算法，巧妙实现;
> 每次任务结束后为我们功能模块提供建设性有意义的后续开发建议；
> 执行php脚本测试必须带超时管理，每个脚本执行时间不能超过10秒；
> 测试/编译产物必须删除；
> **AI 防幻觉规范**：必须阅读 [AI_ANTI_HALLUCINATION_GUIDE.md](AI_ANTI_HALLUCINATION_GUIDE.md)，遵循其中所有约束；
> 修改代码前必须先读取目标文件上下文，禁止凭记忆/推测写代码；
> 代码实现/脚本测试不考虑Fiber和生成器/spl；
> Zig 0.15.2 API 对照表见防幻觉规范 1.2 节，ArrayList/HashMap 必须传 allocator；

## 输出格式契约
- 代码块必须带语言标记 + 文件名  
- 架构图使用 Mermaid；时序图必须含「调用链超时」标注  
- 所有建议按「优先级（P0/P1/P2）+ 影响面 + 落地成本」三列表格呈现  
- 生成提交消息要以中文呈现

## 输出任务总结 【当用户输入指令才要生成】
每个大规模任务完成后都要为我们生成一个变更摘要文档 `docs/{{类型}}/{{DATE}}/{{模块名称}}_{{His}}_{{HASH}}.md`，要求包含如下结构 ⚠️ AI禁止阅读：
```
1. 高层摘要(TL;DR)
2. 影响范围 (表格/图表/结构化/影响描述)
3. 核心变更 (表格/图表/结构化/变更描述)
4. 可视化概览 (代码与逻辑映射)【mermaid视图:Code & Logic Map】
   - 业务模块/业务目标/分层架构图
   - 业务流程 
   - 执行流程
   - 变更点业务相关业务代码逻辑/从上层到底层时序图
5. 详细变更分析 (表格/图表/结构化)
   - 端/模块层/业务层/数据层等
   - 涉及文件列表
   - 变更点描述
6. 影响与风险评估
   - 是否破坏式变更
   - 变更影响范围及明细
   - 需要特别注意的点
   - 复测路径（体验流程）
7. 是否有遗留问题/潜在问题
8. 后续开发/优化建议
```