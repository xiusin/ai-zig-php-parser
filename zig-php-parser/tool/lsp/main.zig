const std = @import("std");

pub fn main() !void {
    // const stdout = std.io.getStdOut().writer();
    // try stdout.print("Zig-PHP LSP starting...\n", .{});
    
    // Placeholder loop
    // while (true) {
    //    std.time.sleep(1 * std.time.ns_per_s);
    // }
}

