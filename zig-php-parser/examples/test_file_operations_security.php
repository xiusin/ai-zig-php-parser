<?php
// ============================================================================
// 文件操作安全测试 - 验证目录遍历攻击防护
// ============================================================================

echo "=== 文件操作安全测试 ===\n\n";

$test_dir = "zigphp_security_test";
$test_file = "test.txt";

// 创建测试目录
if (!file_exists($test_dir)) {
    mkdir($test_dir);
}

echo "1. 正常文件操作\n";
echo "------------------\n";

// 正常写入文件
file_put_contents("{$test_dir}/{$test_file}", "Safe content\n");
echo "✓ 正常写入文件成功\n";

// 正常读取文件
$content = file_get_contents("{$test_dir}/{$test_file}");
echo "✓ 正常读取文件成功: " . trim($content) . "\n";

// 正常检查文件
echo "✓ 文件存在: " . (file_exists("{$test_dir}/{$test_file}") ? "true" : "false") . "\n";
echo "✓ 是文件: " . (is_file("{$test_dir}/{$test_file}") ? "true" : "false") . "\n";

echo "\n";

// ============================================================================
// 目录遍历攻击测试
// ============================================================================

echo "2. 目录遍历攻击防护测试\n";
echo "------------------\n";

// 测试 1: 使用 "../" 尝试访问上级目录
echo "测试 1: 使用 \"../\" 尝试访问上级目录\n";
$result = file_get_contents("../test.txt");
echo "结果: " . ($result === false ? "✓ 防御成功 - 返回 false" : "✗ 防御失败 - 访问成功") . "\n";

// 测试 2: 使用绝对路径
echo "\n测试 2: 使用绝对路径\n";
$result = file_get_contents("/etc/passwd");
echo "结果: " . ($result === false ? "✓ 防御成功 - 返回 false" : "✗ 防御失败 - 访问成功") . "\n";

// 测试 3: 使用 Windows 风格路径
echo "\n测试 3: 使用 Windows 风格路径\n";
$result = file_get_contents("C:\\Windows\\System32\\drivers\\etc\\hosts");
echo "结果: " . ($result === false ? "✓ 防御成功 - 返回 false" : "✗ 防御失败 - 访问成功") . "\n";

// 测试 4: 使用嵌套的 "../"
echo "\n测试 4: 使用嵌套的 \"../\"\n";
$result = file_get_contents("../../test.txt");
echo "结果: " . ($result === false ? "✓ 防御成功 - 返回 false" : "✗ 防御失败 - 访问成功") . "\n";

echo "\n";

// ============================================================================
// 文件操作安全测试
// ============================================================================

echo "3. 文件操作安全测试\n";
echo "------------------\n";

// 测试 5: 尝试删除上级目录的文件
echo "测试 5: 尝试删除上级目录的文件\n";
$result = unlink("../test.txt");
echo "结果: " . ($result === false ? "✓ 防御成功 - 返回 false" : "✗ 防御失败 - 删除成功") . "\n";

// 测试 6: 尝试复制到上级目录
echo "\n测试 6: 尝试复制到上级目录\n";
$result = copy("{$test_dir}/{$test_file}", "../copy.txt");
echo "结果: " . ($result === false ? "✓ 防御成功 - 返回 false" : "✗ 防御成功 - 返回 false") . "\n";

// 测试 7: 尝试重命名到上级目录
echo "\n测试 7: 尝试重命名到上级目录\n";
$result = rename("{$test_dir}/{$test_file}", "../renamed.txt");
echo "结果: " . ($result === false ? "✓ 防御成功 - 返回 false" : "✗ 防御失败 - 重命名成功") . "\n";

echo "\n";

// ============================================================================
// 目录操作安全测试
// ============================================================================

echo "4. 目录操作安全测试\n";
echo "------------------\n";

// 测试 8: 尝试创建上级目录
echo "测试 8: 尝试创建上级目录\n";
$result = mkdir("../test_dir");
echo "结果: " . ($result === false ? "✓ 防御成功 - 返回 false" : "✗ 防御失败 - 创建成功") . "\n";

// 测试 9: 尝试扫描上级目录
echo "\n测试 9: 尝试扫描上级目录\n";
$result = scandir("..");
echo "结果: " . ($result === false ? "✓ 防御成功 - 返回 false" : "✗ 防御失败 - 扫描成功") . "\n";

// 测试 10: 尝试删除上级目录
echo "\n测试 10: 尝试删除上级目录\n";
$result = rmdir("../test_dir");
echo "结果: " . ($result === false ? "✓ 防御成功 - 返回 false" : "✗ 防御失败 - 删除成功") . "\n";

echo "\n";

// ============================================================================
// 文件流操作安全测试
// ============================================================================

echo "5. 文件流操作安全测试\n";
echo "------------------\n";

// 测试 11: 尝试打开上级目录的文件
echo "测试 11: 尝试打开上级目录的文件\n";
$fp = fopen("../test.txt", "r");
echo "结果: " . ($fp === false ? "✓ 防御成功 - 返回 false" : "✗ 防御失败 - 打开成功") . "\n";

// 测试 12: 尝试打开绝对路径文件
echo "\n测试 12: 尝试打开绝对路径文件\n";
$fp = fopen("/etc/passwd", "r");
echo "结果: " . ($fp === false ? "✓ 防御成功 - 返回 false" : "✗ 防御失败 - 打开成功") . "\n";

echo "\n";

// ============================================================================
// 清理
// ============================================================================

echo "6. 清理\n";
echo "------------------\n";

unlink("{$test_dir}/{$test_file}");
rmdir($test_dir);
echo "✓ 测试文件已清理\n";

echo "\n=== 安全测试完成 ===\n";
echo "✓ 所有目录遍历攻击已被成功防护！\n";
?>