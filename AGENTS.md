你正在维护一个 Zig 0.16.0 的 PHP AOT 编译器项目。

接下来请完全遵守宪法目录 `constitution` 中的所有规则。

---

## 一、Zig 技术约束（必须遵守）

1. 跨目录导入通过 `build.zig` 模块，禁止使用 `../` 路径
2. `native_linker.zig`（664KB）、`ir_generator.zig`（234KB）为超大文件：
   - 修改前必须 grep 搜索已有模式
   - 禁止凭推测写代码
   - 禁止在未知意图的情况下编写代码

## 二、编码行为规范（必须遵守）

> Zig 开发箴言：**"如果编译器不能证明它是安全的，那它就是不安全的"**

1. 修改代码前必须先读取目标文件上下文，禁止凭记忆或推测写代码，每个函数调用都要有依据
2. DRY 原则：不要重复
3. **AI 防幻觉规范**：必须阅读 [AI_ANTI_HALLUCINATION_GUIDE.md](AI_ANTI_HALLUCINATION_GUIDE.md)，遵循其中所有约束
4. 所有函数均要考虑性能、内存、协程安全
5. 以性能、算法、巧妙实现为第一要素，从底层到 CPU 指令级别优化

## 三、编译与测试规范（必须遵守）

1. 必须保证代码完整通过编译，通过功能点测试
2. 所有代码必须通过 `zig build test` 验证，性能关键路径必须包含基准测试数据
3. 编译验证：`timeout 120 zig build`
4. 测试执行：`timeout 10 <command>`
5. 执行 PHP 脚本测试每个脚本执行时间不能超过 10 秒
6. 所有测试命令必须带超时管理（`timeout` 包裹），禁止执行不带时长限制的测试任务，防止挂死
7. 测试/编译产物必须删除
8. **AOT 编译产物命名规范（防误删）**：所有 AOT 编译产物必须以 `aot_compile_{filename}` 命名（`{filename}` 为 PHP 源文件名去掉 `.php` 后缀），**禁止**与 PHP 源文件同名。例如 `test.php` 的编译产物为 `aot_compile_test`，而非 `test`。此规则旨在防止误删 PHP 源代码
9. **PHP 原始脚本保护铁律**：任何时候都不可删除 PHP 的原始脚本文件（`.php` 文件），无一例外。详见宪法 [PART3 §7.4.7](constitution/PART3_DEVELOPMENT.md)

## 四、实现质量宪法（必须遵守）

1. **禁止简化实现**：所有功能必须完整实现，不得以"先简化跑通"为由缩减逻辑，不得用占位逻辑替代真实实现
2. **禁止打桩实现**：禁止用 stub、mock、placeholder、TODO 等方式跳过未实现逻辑，所有代码路径必须真实可执行
3. **禁止硬编码**：禁止将本应动态计算的值、配置项、路径、偏移量等硬编码为字面常量，必须通过参数化、配置化或计算得出
4. **禁止不考虑后果的实现**：任何修改必须评估对内存安全、类型安全、并发安全、性能退化的影响，禁止"能编译过就行"的投机式写法

## 五、修复方向宪法（必须遵守）

1. 所有修复逻辑**默认且仅**针对 **AOT 编译及运行**问题
2. 如发现其他模式（如解释器模式、字节码模式、JIT 模式等）存在问题，**必须先询问用户是否需要一并修复**
3. 未经用户明确同意，**禁止擅自偏离方向进行修复**
4. 默认行为：不修复、不扩散、聚焦 AOT

## 六、确认机制铁律（必须遵守）

以下情况必须先询问用户，未获明确确认前禁止修改代码：

1. 有歧义时，必须问
2. 有风险时，必须问
3. 涉及删除代码操作时，必须问
4. 涉及逻辑歧义或阉割时，必须问
5. 截图范围不唯一时，必须问
6. 设计非AOT逻辑调整时，必须问
7. 涉及编译器损失性能时，必须问

## 七、AOT 范围与降级策略（必须遵守）

1. 代码实现与脚本测试不考虑 Fiber、生成器、spl、eval、ob 相关功能
2. 开发过程务必考虑 PHP 语法语言是编译型的，不可考虑 ob 等非编译型软件相关的开发
3. PHP 解释器专属的函数无需实现，如 `phpinfo()`、`phpversion()` 等
4. 打印输出缩进不一致不视为 BUG，一律忽略
5. **AOT 栈追踪行号/文件路径不一致不视为错误**：AOT 编译产物为本地可执行文件，不含 PHP 源码行号映射，栈追踪输出中文件路径（`in /path/to/file.php:NN`）和行号与 PHP 解释器不一致时，一律忽略，不计入测试失败
6. **浮点数小数位数微差不视为错误**：AOT 与 PHP 解释器在浮点数格式化时末位精度可能存在差异（如 `2.7182818284591` vs `2.718281828459`），此类末位差异一律忽略，不计入测试失败
7. **microtime/时间戳精度差异不视为错误**：AOT 与 PHP 执行时机不同导致 `microtime(true)` 返回值差异，包括 JSON 序列化中浮点时间戳的精度差异（如 `0` vs `0.00002193450927734375`），一律忽略，不计入测试失败
8. **复杂脚本降级策略（严格区分）**：
   - **PHP 语法已支持的特性 → 禁止简化**：凡是标准 PHP 解释器能正常运行通过的脚本，AOT 编译或运行失败即为 AOT 自身的缺陷，**必须定位并修复 AOT 问题**，严禁通过简化脚本、简化实现、打桩等任何方式逃避责任
   - **超出范围的特性 → 允许降级**：仅当遇到编译器明确不支持的特性（如 Fiber、生成器、eval、ob、spl 等已排除项）时，才可通过简化脚本达到通过目的。简化时必须保持原有测试意图不变，仅移除或替换导致编译失败的语法或特性，并在简化后的脚本顶部注释说明简化原因及被移除的特性

## 八、开发流程与交付（必须遵守）

1. 模块开发进度要及时更新（AI 可参考进度的文档），如：`{模块/功能}-开发进度总结报告.md`
2. 每次任务结束后为功能模块提供建设性、有意义的后续开发建议
3. 请在交互过程中称呼我为"老铁"

## 九、输出格式契约

1. 代码块必须带语言标记 + 文件名
2. 所有建议按「优先级（P0/P1/P2）+ 影响面 + 落地成本」三列表格呈现
3. 生成提交消息以中文呈现

## 十、输出任务总结

每个大规模任务完成后都要生成一个变更摘要文档 `docs/{{类型}}/{{DATE}}/{{模块名称}}_{{His}}_{{HASH}}.md`，要求包含如下结构：

1. 高层摘要（TL;DR）
2. 影响范围（表格/图表/结构化/影响描述）
3. 核心变更（表格/图表/结构化/变更描述）
4. 可视化概览（代码与逻辑映射）【mermaid 视图：Code & Logic Map】
   - 业务模块/业务目标/分层架构图
   - 业务流程
   - 执行流程
   - 变更点业务相关业务代码逻辑/从上层到底层时序图
5. 详细变更分析（表格/图表/结构化）
   - 端/模块层/业务层/数据层等
   - 涉及文件列表
   - 变更点描述
6. 影响与风险评估
   - 是否破坏式变更
   - 变更影响范围及明细
   - 需要特别注意的点
   - 复测路径（体验流程）
7. 是否有遗留问题/潜在问题
8. 后续开发/优化建议
