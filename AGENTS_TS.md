# AGENTS.md

## 项目：ts-aot — 基于 Zig 的 TypeScript/JavaScript AOT 编译器

一款生产级预先编译器，将 TypeScript/JavaScript 编译为本地机器码，性能超越 Node.js (V8)。

## 绝对约束（不可协商）

1. **无外部 JS 运行时**：严禁使用 V8、QuickJS、JavaScriptCore、SpiderMonkey 或其他任何 JS 引擎。全部运行时（GC、内置对象、类型系统）**必须**在 Zig 中从头实现。

2. **纯 Zig 实现**：所有源代码**必须**使用 Zig（最新稳定版）。禁止依赖 C、C++、Rust 或其他语言。平台 libc 仅用于系统调用，是唯一例外。

3. **性能门槛**：每个基准测试**必须**证明 AOT 编译代码不比等效 Node.js 执行慢。任何基准测试未通过此测试，实现即视为失败。目标：在所有基准测试中显著快于 Node.js。

4. **测试先行开发**：每个组件（词法分析器、解析器、IR、优化器、代码生成器、运行时、内置对象）**必须**有全面的单元测试。未通过测试的代码不得合并。

5. **无 LLVM 依赖**：代码生成器**必须**为自定义实现。禁止 LLVM 绑定、QBE、Cranelift。编译器直接生成本地机器码。

6. **ES2025 语言规范对齐**：实现的 JS/TypeScript 语法和语义**必须**完整对齐 ECMAScript 2025（ECMA-262 第16版）规范。包括但不限于：所有内置对象（RegExp、Promise、Intl、TypedArray、SharedArrayBuffer、Atomics、WeakRef、FinalizationRegistry 等）、语法特性（decorators 提案阶段、import attributes、JSON modules 等）、以及最新 stage 4 提案。禁止选择性实现子集后标注"对齐 ES2025"——要么全实现，要么声明的版本号要如实反映覆盖范围。

## 架构规则

7. **流水线完整性**：Lexer → Parser (CST→AST) → 语义分析器 → IR 构建器 → 优化器 → 代码生成器。每个阶段**必须**是纯变换，阶段之间无循环依赖。

8. **SSA 形式 IR**：中间表示**必须**为静态单赋值形式（Static Single Assignment），包含恰当的 phi 节点和控制流图。禁止无 SSA 的栈式 IR 或三地址码。

9. **多趟优化器**：优化**必须**作为独立的趟次运行。趟次顺序：ConstantFolding → StrengthReduction → Inlining → Monomorphization → GVN → LICM → DCE → EscapeAnalysis → LoadStoreElimination → BranchFolding。每趟必须可独立测试。

10. **NaN-boxing 值表示**：所有 JS 值**必须**使用 64 位 NaN-boxing 表示。指针标记（Pointer tagging）对 Smi 可接受，但主方案是 NaN-boxing。这使得值传递对寄存器友好。

11. **基于 Shape 的对象模型**：属性访问**必须**使用 Shape（隐藏类）配合内联缓存路径。禁止纯哈希表的对象实现。单态化访问的快速路径**必须**是直接偏移量的 load/store。

12. **分代 GC**：垃圾回收器**必须**是分代式的，使用 bump-pointer 新生代分配。Minor GC 使用复制收集（Cheney semispace）。Major GC 使用标记压缩（mark-compact）。

## 代码质量规则

13. **无不安全模式**：无未定义行为。所有指针操作在调试模式下必须进行边界检查。发布模式仅能在安全证明后省略检查。

14. **Zig 最佳实践**：在适当场景使用 `comptime` 进行代码生成。使用 `errdefer` 进行清理。使用 Arena 分配器进行阶段局部分配。显式分配器传递（无全局分配器）。

15. **错误处理**：所有可失败操作返回 error union。解析错误收集在 DiagnosticBag 中，不立即返回。词法分析器和解析器**必须**从错误中恢复并继续（绝不在无效输入时崩溃）。

16. **文档**：每个公开函数、结构体和模块**必须**有文档注释（`///`）。复杂算法（GC、寄存器分配、内联启发式）**必须**有解释性注释。

## 测试规则

17. **单元测试位置**：单元测试与实现位于同一文件中（Zig 惯例：文件底部的 `test { }` 块）。集成测试位于 `tests/` 目录下。

18. **快照测试**：解析器输出和 IR 变换**必须**有快照测试。序列化为稳定格式（ZON 或自定义格式），并与检入的预期输出对比。

19. **模糊测试**：词法分析器和解析器**必须**有模糊测试，提供随机字节并验证不会崩溃。

20. **测试时长上限**：每个单元测试或集成测试的独立运行时长**必须**控制在 10 秒以内。超出 10 秒的视为测试质量不合格，**必须**拆分或优化。CI 将被配置为超时阈值 10 秒。

## 构建与工具

21. **构建系统**：项目根目录单一 `build.zig`。目标平台：`x86_64-linux`、`aarch64-macos`（初始）。构建模式：Debug、ReleaseSafe、ReleaseFast、ReleaseSmall。

22. **CLI 设计**：二进制名为 `ts-aot`。子命令：`compile`、`run`、`test`、`bench`、`build`。使用 `--target` 指定交叉编译目标，`--opt-level` 指定优化级别。

---

# Zig 开发最佳实践宪法

最高指导原则，所有代码必须遵守。违反本宪法的 PR 不予合并。

## 第一章：反幻觉

1. **读后再改**：修改文件前必须先 `read_file` 完整阅读其当前内容，禁止凭记忆或猜测修改。
2. **先搜再用**：引用类型/函数/常量前，必须用 `grep` 确认其确切名称、签名和模块。
3. **comptime 断言**：涉及布局假设的 `packed/extern struct` 必须在 `comptime {}` 中用 `@sizeOf`/`@alignOf`/`@bitSizeOf` 断言验证内存布局。
4. **枚举显式位宽**：所有枚举必须加位宽标签（`enum(u8)`/`enum(u16)`/etc），禁止裸 `enum`。≤256 变体用 `u8`，≤65536 用 `u16`。
5. **整数明确位宽**：每个整数字段必须指定位宽（`u32`/`u64`/`i32` 等），禁止 `usize` 用于非指针字段。
6. **API 确证**：引用标准库 API 时必须提供确切模块路径，确认该 API 在当前 Zig 版本中可用。
7. **不猜语义**：`defer`/`errdefer`/`comptime` 等语义有疑问时编写最小测试确认，不凭直觉假设。

## 第二章：文件限制

8. **1500 行硬限**：每个 `.zig` 文件（含测试）不得超过 1500 物理行，超限必须拆分子模块。
9. **仅按职责拆分**：拆分依据必须是功能内聚性。禁止按大小/字母/时间/访问修饰符/测试实现分离拆分。每个拆分后文件头部必须有 `//!` 职责注释且职责无交叉。
10. **文件结构**：统一顺序——模块文档 `//!` → `const` 导入 → `pub const` 重导出 → 主实现 → 私有辅助函数 → 文件尾部 `test` 块。
11. **接近即拆**：文件接近 1500 行时必须在提交前拆分，不允许"下次再拆"。

## 第三章：职责拆分

12. **单向依赖链**：编译流水线必须严格遵循 `Lexer→Parser→Sema→IR→Optimizer→CodeGen` 方向，禁止后级引用前级。
13. **模块独立编译**：每个顶层模块必须能独立编译，通过 `build.zig` 的 `addModule` 声明依赖，禁止文件相对路径引用。
14. **单 struct 单职责**：每个文件最多一个核心 struct，`init`/`deinit` 成对。
15. **PhaseAllocator 隔离**：编译阶段各自拥有 PhaseAllocator，阶段结束统一释放，禁止跨阶段共享分配内存。
16. **测试随实现**：单元测试在源文件尾部，集成测试在 `tests/`。拆分文件时测试随实现移动。

## 第四章：内存安全

17. **显式分配器传递**：所有分配内存的函数必须接受 `std.mem.Allocator` 参数，禁止全局分配器。
18. **errdefer 覆盖**：`init` 等可能部分失败的函数，每个资源分配后立即放置 `errdefer` 清理，确保部分分配不泄漏。
19. **defer 临时分配**：函数内部临时分配后立即 `defer alloc.free`。
20. **init/deinit 必成对**：每个持有资源的 struct 必须同时提供 `init` 和 `deinit`。
21. **所有权显式传递**：值转移用 `return result; self.field = null;` 交出所有权；指针借用确保不超过所有者生命周期；禁止返回栈变量地址。
22. **指针类型精确选择**：定长数组用 `[]T`，指针算术用 `[*]T`（需边界管理），单对象用 `*T`，对齐不明用 `@alignCast`。
23. **@intFromPtr/@ptrFromInt 显式转换**：指针↔整数转换必须用 `@intFromPtr`/`@ptrFromInt`。

## 第五章：算法优先

24. **算法文档化**：每个核心算法/优化 pass 必须在文件头部用 `//!` 描述算法名称、复杂度、数据结构和关键步骤。
25. **复杂度标注**：每个公开数据结构上方标注其关键操作的时空复杂度。
26. **magic number 命名**：所有阈值/大小/深度限制必须定义为命名的 `const` 常量并附注释说明理由。
27. **低复杂度优先**：优先 O(n)/O(n log n)，使用 O(n²) 必须在注释中说明理由。
28. **高频查询缓存**：符号/类型等高频率查询必须预填充缓存以提供 O(1)/O(log n) 查询。
29. **算法测试**：每个算法单元测试必须包含 happy path + 边界 case + 算法不变性验证。

## 第六章：内存优化

30. **packed struct 紧凑布局**：多 bool/枚举字段的 struct 使用 `packed struct(uN)` 并在 comptime 验证大小。
31. **枚举最小位宽**：优先 `enum(u8)`/`enum(u16)` 而非默认 `enum`(usize)。
32. **u32 索引替代指针**：非 `?` 空值场景用 `u32` 索引（4 字节）替代 8 字节指针引用。
33. **Arena 分配**：编译阶段内临时分配用 ArenaAllocator/PhaseAllocator，结束后统一释放。
34. **ArrayListUnmanaged 优先**：struct 字段中用 `ArrayListUnmanaged(T)` 替代 `ArrayList(T)`（省 8 字节分配器指针）。
35. **预分配容量**：已知元素数量范围时用 `ensureTotalCapacity` 预分配减少 reallocation。
36. **避免小对象堆分配**：<64 字节对象优先值语义嵌入父结构，或用 MemoryPool 池化。
37. **编译期优先强制**：一切支持 comptime 生成（泛型特化、类型计算）或编译期优化（常量折叠、DCE）的功能必须用编译期方案。仅在输入编译期不可知 + 二进制膨胀 >2× 且性能收益 <5% 时例外，且必须写 `// NOTE: 无法使用 comptime 因为 ...` 注释。
38. **内联克制**：仅 ≤5 指令或含 comptime 参数的函数用 `inline`，过大内联导致代码膨胀和 I-cache 未命中。
39. **引用局部性**：热路径数据连续排列（ArrayList .items 切片），不用链表遍历。

## 第七章：开发纪律

40. **收益优先于难度**：收益中等及以上（影响 ≥5% 用户代码路径 或 benchmark≥1%）的功能**必须实现**，不许以难度为借口跳过。
41. **禁止打桩**：禁止 `unreachable`、`@panic("TODO")`、`return error.NotImplemented`、空函数体、`// TODO` 未实现功能点作为占位。
42. **禁止空实现**：列入开发计划的函数/pass 必须完整实现，禁止空循环体、空 switch case、`undefined` 字段、占位返回值。
43. **禁止硬编码替代**：禁止用硬编码值/魔数结果/常量分支假装实现逻辑。临时 spike 代码必须 `// SPIKE ONLY` 标注且不进主分支。
44. **歧义即问**：对需求有任何歧义必须问客户，禁止猜测或默默选方案。提问要提供 A/B 方案让客户做选择题。
45. **开发前存量排查**：新功能前必须全面检索代码库相同/相似功能（同模块、兄弟模块、runtime、builtins、IR Opcode、AST NodeKind），列出所有发现并说明差异，禁止盲目开发。

46. **多轮自审查防遗漏**：团队任务或 spec 任务（含 PRD、功能规格、技术设计文档中定义的需求），在实现过程中**必须经过多轮自审查**，确保：
    - **无遗漏**：逐条核对 spec 中的每个功能点/子任务，确认全部覆盖，无一跳过
    - **不偏题**：审查实现是否严格遵循 spec 定义，禁止引入未授权的额外功能或偏离原定方案
    - **业务闭合**：验证功能从入口到出口的全链路是否完整闭合——包括错误路径、边界条件、资源释放、副作用处理。任何未闭合的路径（如未处理的错误返回、未释放的资源、未覆盖的分支）必须在提交前修复
    - **审查方式**：每完成一个功能子集，暂停并对照 spec 逐项核查。完成全部功能后，再进行一次全量审查。审查结果以自查清单形式写入 PR 描述或 commit 消息中

47. **测试正确性优先**：执行测试和验证时，**结果正确性**是首要且不可妥协的标准，不能用"跑了/通过了/有速率"替代正确性验证。必须逐项核验测试输出与预期结果一致，确认功能逻辑正确后方可关注性能指标。禁止仅执行测试但不检查结果正确性就视为"验证通过"。

48. **必须对比 Node.js 结果**：所有编译执行的 JS 代码，其输出结果**必须与 Node.js 执行结果逐字一致**。包括标准输出、退出码、错误信息和异常行为。禁止用"逻辑相似""大致对得上"代替精确对比。验证方式：同一 JS 脚本分别在 AOT 编译运行和 Node.js 运行，diff 两者输出，确认完全一致。

    **特例**：随机数（`Math.random()`）、时间戳（`Date.now()`、`new Date()`）、唯一标识（`crypto.randomUUID()`）等因执行时机/环境不同导致每次运行结果必然不一致的逻辑，不要求逐字一致。但必须验证：① 值在合理范围内（如随机数在 [0,1) 内）；② 多次执行结果不同（证明非伪恒定）；③ 程序流程和分支行为与 Node.js 一致。

49. **零内存泄漏**：每次 `alloc` 必有对应 `free`，`errdefer` 覆盖所有失败路径，禁止 `@ignoreMemoryLeak`，CI 含泄漏检测。已知泄漏必须修复或开 Issue 加 `// LEAK: Issue-N` 注释。
50. **禁止简化实现**：禁止为"快速实现"忽略边界检查/错误路径/竞态/类型安全/流水线交互。违反立即禁止合并。
51. **编译产物清除**：`zig-out/`、`.o`、`.elf` 等产物不用时必须清除，禁止提交版本控制。
52. **test_ 文件规范**：临时验证 `.zig` 文件必须以 `test_` 开头放在 `tests/` 下，用完后删除。核心代码不得写在 `test_` 文件中。

53. **运行时死循环/泄漏检测**：非阻塞程序运行过程中，执行超过 **1分钟** 或内存占用超过 **1GB** 时，必须视为**疑似死循环或严重内存泄漏**。开发者必须立即中断执行并审查确认：① 检查是否存在未覆盖的 exit condition / break 路径；② 检查是否存在分配后未释放的循环引用或遗忘递减引用计数；③ 检查是否每个 `alloc` 都有对应的 `free` 路径。确认排除后才能在代码中标注 `// SAFE: 已审查，非死循环/泄漏` 并附上审查依据。

54. **禁止简化方案**：不得为了快速实现而不考虑后果地实施简化方案。任何跳过边界检查、错误路径、类型安全、资源释放或流水线交互的"快速实现"**严禁合并**。若有合理的简化理由，必须在代码中标注 `// SIMPLIFY: 原因` 并附带完整的影响分析。

## 第八章：命名规范

55. 类型 `PascalCase`（struct/enum/union/opaque）
56. 函数/方法 `snake_case`
57. 顶层/编译期常量 `SCREAMING_SNAKE_CASE`
58. 私有标识符用相同大小写规则，**不使用** `_` 前缀
59. import 别名：子模块用 `_mod` 后缀（`expr_mod`），标准库子模块简短全小写（`mem`），兄弟模块用目录同名（`IR`、`Ast`）
60. 字段/局部变量 `snake_case`，布尔用 `is_`/`has_`/`can_` 前缀

61. **文件名规范**：文件名**必须**遵循 Zig 官方社区推荐风格——全小写 `snake_case.zig`。禁止使用大写字母、连字符、空格或随意缩写。文件名**必须**有意义，能反映文件的核心职责，禁止无意义命名（如 `a.zig`、`temp.zig`、`misc.zig`）。测试文件统一用 `test_` 前缀。

## 第九章：错误与返回值

62. 精确 error set 优先，禁止 `anyerror!T` 作为函数返回类型。
63. 错误名 `UpperCamelCase`（`error.UnexpectedToken`）。
64. 禁止吞没错误：`catch unreachable`（除非加 `// SAFE:` 注释证明安全）、`catch {}`、`catch|err|@panic`、`catch|err|` 仅打印不恢复。
65. `?T` 用于"未找到是正常流程"（查找、可选参数、可选配置），`error!T` 用于"预期成功但环境/输入不满足"。
66. `try` 优先传播错误；`errdefer` 在每个分配后立即放置，多个 `errdefer` 按栈式逆序 unwind。

## 第十章：const 优先

67. 变量声明 `const` 优先，`var` 必须有修改理由（循环计数器、累加器、状态机）。
68. 方法接收者：getter 用 `*const Self`，mutator 用 `*Self`。
69. 函数参数：只读用 `[]const T`/`*const T`，需修改用 `[]T`/`*T`。
70. 禁止 magic sentinel（`-1`/`0xFFFFFFFF`）替代 `?T`。
71. `undefined` 仅用于 init 中立即覆写的临时占位或 hot path 经测量证明有收益的场景，禁止将未初始化字段暴露给外部。

---

*本宪法于创建之日起生效，随项目演化可经集体评审修订。每项条款均从现有代码库（`/Users/tuoke/Desktop/worktree/ts-aot/src`）的实战模式中提炼而来。*

## 原则一：先思考再编码（Think Before Coding）
不假设。不隐藏困惑。暴露权衡。
- 显式声明假设。如果不确定，先问再动手。
- 如果存在多种理解方式，列出所有解读——不要默默选一个就跑。
- 如果存在更简单的方案，直说并推荐。该push back时就push back。
- 如果有什么不清楚的，立即停下。说出哪里不清楚，然后提问。

## 原则二：简洁优先（Simplicity First）
解决问题的最小代码量。不做投机性设计。
- 不做超出需求范围的功能。
- 不为只用一次的代码创建抽象层。
- 不做未被要求的"灵活性"或"可配置性"。
- 不为不可能出现的场景编写错误处理。
- 如果你写了200行而50行就能搞定，重写它。
- 自检标准：一个资深工程师看到这段代码会说"过度工程化了"吗？如果会，简化它。

## 原则三：外科手术式修改（Surgical Changes）
只改必须改的。只清理你自己造成的混乱。
- 不要"顺手改善"相邻的代码、注释或格式。
- 不要重构没有问题的代码。
- 匹配现有风格，即使你会用不同的方式写。
- 如果发现无关的死代码，提一嘴就行——不要删。
- 删除你的修改导致的未使用的import/变量/函数。
- 不要删除修改前就存在的死代码，除非被要求。
- 验证标准：每一行改动都应该能直接溯源到用户的需求。

## 原则四：目标驱动执行（Goal-Driven Execution）
定义成功标准。循环直到验证通过。
- "加个校验" → "为非法输入写测试，然后让测试通过"
- "修这个bug" → "写一个复现bug的测试，然后让测试通过"
- "重构X" → "确保重构前后测试全部通过"
- 多步骤任务必须陈述简要计划：[步骤] → 验证: [检查项]
- 强成功标准让你能独立循环。弱标准需要不断澄清。

## 原则五：功能设计原样实现（Implement As Designed）
设计即契约，实现必须忠实于设计，禁止自作主张融合或改写。
- 用户提供的设计方案、PRD、功能规格或实现指令，必须**原样复刻**，不得将多个独立功能点擅自合并、糅合、简化或重新组织。
- 不得以「更优雅」「更简洁」「更合理」为由，对已确认的设计进行任何形式的改写或重构。
- 实现时必须与设计文档中的功能点**一一对应**，每个功能点独立实现，不得遗漏或合并。
- 如果设计文档存在歧义或模糊之处，**必须停下来向用户确认**，而不是自行推断并实现。
- 如果在实现过程中发现设计存在问题或需要调整，**必须先向用户说明原因并获得明确授权**后才能变更。
- 禁止以「减少重复代码」「用户体验更好」「性能更优」为由擅自修改设计，在未确认的情况下对设计做任何「改进」。

*本宪法于创建之日起生效，随项目演化可经集体评审修订。每项条款均从现有代码库（`/Users/tuoke/Desktop/worktree/ts-aot/src`）的实战模式中提炼而来。*
