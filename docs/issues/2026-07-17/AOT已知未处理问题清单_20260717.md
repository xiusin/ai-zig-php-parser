# AOT 已知未处理问题清单

> 更新时间：2026-07-17（第十一轮修复后）
> 当前总状态：全量 61 脚本 PASS=61 DIFF=0 FAIL=0

---

## 一、当前测试状态

```
=== pass: 37/37 PASS ===
=== fail_runtime: 17/17 PASS ===
=== fuzzy_scripts_73: 7/7 PASS ===
总计: 61 PASS=61 DIFF=0 FAIL=0
```

## 二、可忽略差异（无需修复）

| 脚本 | 差异原因 | 忽略规则 |
|------|----------|----------|
| c016 | AOT 栈追踪行号/文件路径不一致 | 宪法七§5 |
| f100 | Deprecated `${var}` 字符串警告 | 警告差异忽略 |

## 三、已完成的功能性问题

### P1（中优先级）— 已全部完成

| 编号 | 问题 | 状态 | 完成轮次 | 修复摘要 |
|------|------|------|----------|----------|
| ~~P1-1~~ | `static` 后期静态绑定 | ✅ 完成 | 第九轮 | `resolveTypeSpecials` 不替换 static，`checkSingleType` 运行时通过 `getCurrentCalledClass()` 解析 |
| ~~P1-2~~ | 组合类型中 self/parent 替换 | ✅ 完成 | 第九轮 | 新增 `resolveTypeSpecials` 逐组件替换 self/parent |
| ~~P1-3~~ | 参数类型检查扩展到全局函数 | ✅ 完成 | 第九轮 | `IR.Function` 新增 param_types 字段，`generateFunctionDecl` 填充，`native_linker` 生成检查代码 |
| ~~弱类型~~ | PHP 弱类型模式下的类型强制转换 | ✅ 完成 | 第九轮 | `checkSingleType` 添加数字字符串→int/float、int/float/bool→string、float/string→bool 转换 |
| ~~P1-4~~ | GC 增量式收集 | ✅ 完成 | 第十一轮 | `gcCollectCycles` 增量批次处理 `GC_BATCH_SIZE=64`，每次最多处理 64 个根，未处理的保留到下次 |
| ~~P1-值转换~~ | 弱类型值实际转换 | ✅ 完成 | 第十一轮 | 新增 `php_coerce_value`，参数加载时对标量类型做值转换；nullable 参数 null 时不转换；补全 bool→int/float、float→int 弱类型规则 |

### P2（低优先级）— 已全部完成

| 编号 | 问题 | 状态 | 完成轮次 | 修复摘要 |
|------|------|------|----------|----------|
| ~~P2-1~~ | callable 深度验证 | ✅ 完成 | 第十轮 | `checkSingleType` callable 分支复用 `php_is_callable` 做函数名存在性/array格式/__invoke 深度验证 |
| ~~P2-2~~ | 四级以上嵌套数组写回 | ✅ 完成 | 第十轮 | push 路径保存中间层 `array_ensure` 寄存器，逐层写回修复 COW 克隆后中间层丢失 |

### 系统债务（来自重复实现审查，2026-07-16）

| 编号 | 问题 | 规模 | 落地成本 | 文档位置 |
|------|------|------|----------|----------|
| P2-3 | rt_*.zig 死代码清理 | 26,880 行 100% 死代码 | 低（零风险） | `docs/issues/2026-07-16/系统重复实现详细审查报告与优化方案_20260716.md` |
| P2-4 | runtime/ 目录死代码 | ~11,000 行确认无引用 | 低 | 同上 |
| P2-5 | nanbox_abi 常量冲突 | shared/ 与 aot/ 版 TYPE 标签冲突 | 中 | 同上 |

## 四、推进优先级建议

1. ~~P1-1~~（static 后期静态绑定）— ✅ 已完成
2. ~~P1-2~~（组合类型 self 替换）— ✅ 已完成
3. ~~P1-3~~（全局函数参数类型检查）— ✅ 已完成
4. ~~P1-4~~（GC 增量式收集）— ✅ 已完成
5. ~~P1-值转换~~（弱类型值实际转换）— ✅ 已完成
6. ~~P2-1~~（callable 深度验证）— ✅ 已完成
7. ~~P2-2~~（四级以上嵌套数组写回）— ✅ 已完成
8. **P2-3/P2-4/P2-5** — 系统债务，用户表示自行处理
